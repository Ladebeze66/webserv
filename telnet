Voici une variété de commandes telnet que vous pouvez utiliser pour interagir avec différents types de services sur Internet ou sur des réseaux locaux. Telnet est un outil très polyvalent qui peut être utilisé pour tester la connectivité et interagir avec divers services en utilisant des ports de communication spécifiques.

1. Connexion de base
Pour vous connecter à un service en utilisant telnet, spécifiez simplement l'adresse du serveur et le port sur lequel vous souhaitez vous connecter :
telnet [adresse] [port]
Exemple :
telnet example.com 80

2. Interaction avec un serveur HTTP
Pour envoyer des requêtes HTTP manuelles via Telnet :
Ouvrez une connexion sur le port 80 :
telnet example.com 80
Tapez votre requête HTTP :
GET / HTTP/1.1
Host: example.com
Appuyez sur Entrée deux fois pour envoyer la requête.

3. Se connecter à un serveur SMTP
Pour tester un serveur de messagerie en utilisant le port SMTP (25) :
Ouvrez Telnet sur le port 25 :
telnet mailserver.com 25
Vous pouvez tester les commandes de base du SMTP comme HELO, MAIL FROM:, RCPT TO:, et DATA :
HELO client.example.com
MAIL FROM: <your-email@example.com>
RCPT TO: <recipient@example.com>
DATA
Subject: Test email via Telnet
This is a test email sent via Telnet.
.
QUIT

4. Connexion à un serveur POP3
Pour se connecter à un serveur POP3 (port 110) et vérifier les e-mails :
Ouvrez Telnet sur le port 110 :
telnet mailserver.com 110
Utilisez les commandes POP3 comme USER, PASS, LIST, RETR :
USER yourusername
PASS yourpassword
LIST
RETR 1
QUIT

5. Envoyer des commandes à un serveur FTP
Pour interagir avec un serveur FTP (port 21) :
Ouvrez Telnet sur le port 21 :
telnet ftpserver.com 21
Tapez les commandes FTP :
USER username
PASS password
LIST
QUIT

6. Quitter Telnet
Pour quitter une session Telnet, utilisez :
Ctrl+] puis tapez quit et appuyez sur Entrée.

Conseils d'utilisation:
Sécurité : Telnet n'est pas sécurisé, car il transmet toutes les données, y compris les mots de passe, en texte clair. Utilisez SSH où c'est possible.
Fonctionnalité : Certaines fonctionnalités avancées de gestion de connexion peuvent ne pas être supportées par Telnet. Pour des tests plus avancés ou sécurisés, envisagez d'utiliser des outils dédiés pour le protocole spécifique que vous testez.